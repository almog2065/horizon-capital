"""Core production utilities: settings, logging, lifecycle.

This package holds the *production-ready* primitives that the rest of the
firm uses. Modules in here intentionally have no upward dependencies on
agents / routes / business logic — they're the base of the dependency
graph.
"""
